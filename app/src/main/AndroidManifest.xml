<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- com.android.vending.BILLING 由 Play Billing Library (billing-ktx) 自動 merge，
         並帶入 Play 所需的 Library 版本；不可在此手動宣告，否則會被視為舊版 AIDL Billing。 -->

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <application
        android:name=".DanjiangApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DanjiangSunSeeker"
        android:usesCleartextTraffic="false"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="33">

        <!-- 真正的 Activity 不再直接掛 LAUNCHER；改由兩個 activity-alias 提供桌面圖示，
             付費後在執行期切換 alias 以更換成「尊榮版」圖示。 -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.DanjiangSunSeeker" />

        <!-- 預設（免費版）桌面圖示，預設啟用。 -->
        <activity-alias
            android:name=".MainActivityDefault"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Pro 桌面入口，預設停用；付費後由 ProIconManager 啟用、並停用預設 alias。
             圖示與預設同底色，僅多疊「PRO」徽章；App 名稱（label）不變。 -->
        <activity-alias
            android:name=".MainActivityPro"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false"
            android:icon="@mipmap/ic_launcher_pro"
            android:roundIcon="@mipmap/ic_launcher_pro_round"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- WorkManager 採用 on-demand 初始化 (DanjiangApp 實作 Configuration.Provider)，
             需移除 androidx.startup 的預設 WorkManagerInitializer，否則會重複初始化。 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- ARCore：標示為可選；舊裝置會自動降級 -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />

        <!-- Google Maps / Elevation 金鑰（同一把） -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}" />

        <!-- AdMob App ID。實際值見 res/values/ads.xml。 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />
    </application>
</manifest>
